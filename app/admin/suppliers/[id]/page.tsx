import { notFound } from "next/navigation";
import { prisma } from "@/lib/db";
import { requireAdminPage } from "@/lib/auth/guards";
import { AdminHeading } from "@/components/admin/admin-shell";
import { AdminSupplierEdit, SupplierStatusControl } from "@/components/admin/admin-actions";
export default async function SupplierInspectPage({params}:{params:Promise<{id:string}>}){await requireAdminPage();const{id}=await params;const supplier=await prisma.supplierCompany.findUnique({where:{id},include:{memberships:{include:{user:true}},coverageAreas:true,categories:{include:{productCategory:true}},subscription:true,assignments:{where:{respondedAt:{not:null}},select:{assignedAt:true,respondedAt:true}}}});if(!supplier)notFound();const avg=supplier.assignments.length?supplier.assignments.reduce((n,a)=>n+((a.respondedAt?.getTime()??a.assignedAt.getTime())-a.assignedAt.getTime()),0)/supplier.assignments.length:null;const coverageDefinition=(area:typeof supplier.coverageAreas[number])=>area.type==="POSTCODE"?`Postcode area ${area.postcodePrefix}`:area.type==="NATIONWIDE"?"All UK postcodes":`${area.radiusMiles} mile radius from ${area.centrePostcode}`;return <><AdminHeading eyebrow={supplier.status} title={supplier.tradingName??supplier.legalName} description={`${supplier.contactEmail} · ${supplier.contactPhone}`} actions={<SupplierStatusControl id={id} status={supplier.status}/>}/><div className="management-grid"><section className="panel form-section"><div className="section-heading"><div><p className="eyebrow">Company record</p><h2>Profile</h2></div></div><dl className="detail-list"><div><dt>Legal name</dt><dd>{supplier.legalName}</dd></div><div><dt>Company number</dt><dd>{supplier.companyNumber??"—"}</dd></div><div><dt>Address</dt><dd>{[supplier.addressLine1,supplier.city,supplier.county,supplier.postcode].filter(Boolean).join(", ")||"—"}</dd></div><div><dt>Average response</dt><dd>{avg?`${Math.round(avg/3600000)} hours`:"No completed responses"}</dd></div><div><dt>Subscription</dt><dd>{supplier.subscription?`${supplier.subscription.planCode} · ${supplier.subscription.status}`:"None"}</dd></div></dl><p className="body-copy">{supplier.summary}</p></section><section className="panel form-section"><div className="section-heading"><div><p className="eyebrow">Matching configuration</p><h2>Categories & coverage</h2></div></div><div className="tag-list">{supplier.categories.map(c=><span className="status-pill" key={c.productCategoryId}>{c.productCategory.name}</span>)}</div><div className="entity-list">{supplier.coverageAreas.map(a=><article className="entity-row" key={a.id}><div><b>{a.label}</b><small>{coverageDefinition(a)} · {a.active?"active":"disabled"}</small></div></article>)}</div><div className="section-subheading">Team</div><div className="entity-list">{supplier.memberships.map(m=><article className="entity-row" key={m.id}><div><b>{m.user.firstName} {m.user.lastName}</b><small>{m.user.email}</small></div><span className="status-pill">{m.role}</span></article>)}</div></section></div><div className="spaced-section"><AdminSupplierEdit supplier={{id:supplier.id,legalName:supplier.legalName,tradingName:supplier.tradingName,contactEmail:supplier.contactEmail,contactPhone:supplier.contactPhone,companyNumber:supplier.companyNumber,vatNumber:supplier.vatNumber,postcode:supplier.postcode,summary:supplier.summary}}/></div></>}
