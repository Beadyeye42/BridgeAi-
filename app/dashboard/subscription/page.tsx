import { CreditCard, ShieldCheck } from "lucide-react";
import { prisma } from "@/lib/db";
import { requireSupplierPage } from "@/lib/auth/guards";
import { PortalPage, identity } from "@/components/dashboard/portal-page";

export const dynamic="force-dynamic";
export default async function SubscriptionPage(){const{session,companyId}=await requireSupplierPage();const company=await prisma.supplierCompany.findUniqueOrThrow({where:{id:companyId},include:{subscription:true}});const sub=company.subscription;return <PortalPage {...identity(session,company)} eyebrow="Billing" title="Subscription" description="View the plan and billing state Bridge AI currently has on record."><div className="management-grid"><section className="panel subscription-detail"><span className="large-icon"><CreditCard size={24}/></span><p className="eyebrow">Current plan</p><h2>{sub?.planCode??"No plan assigned"}</h2><span className={`status-pill ${sub?.status.toLowerCase()??"pending"}`}>{sub?.status??"UNAVAILABLE"}</span><dl><div><dt>Current period</dt><dd>{sub?.currentPeriodStart?.toLocaleDateString("en-GB")??"—"} – {sub?.currentPeriodEnd?.toLocaleDateString("en-GB")??"—"}</dd></div><div><dt>Renewal</dt><dd>{sub?.cancelAtPeriodEnd?"Cancels at period end":"Automatic when billing is active"}</dd></div></dl></section><section className="panel form-section"><div className="section-heading"><div><p className="eyebrow">Billing controls</p><h2>Managed securely</h2></div><ShieldCheck size={20}/></div><p className="body-copy">Payment details are never stored in Bridge AI. Plan changes and payment-method updates are available only after a billing provider customer record has been linked.</p>{sub?.providerCustomerId?<a className="button button-dark" href="/api/billing/portal">Open billing portal</a>:<div className="honesty-note">Billing self-service is not enabled for this account. Contact Bridge AI support to change your plan.</div>}</section></div></PortalPage>}
